DISCORD_TOKEN=
CLIENT_ID=
# Optional: restrict global commands to one channel across the entire bot.
# Otherwise an administrator uses -config globalchannel #channel in a server.
GLOBAL_COMMAND_CHANNEL_ID=
DATABASE_URL=postgresql://vengeful:change-me@localhost:5432/vengeful?schema=public
COMMAND_PREFIX=-
# Optional guild-only command registration for testing.
COMMAND_GUILD_ID=
SYNC_INTERVAL_MINUTES=15
LOG_LEVEL=info
